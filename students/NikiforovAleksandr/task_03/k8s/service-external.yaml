apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: state01
  labels:
    app: redis
    variant: "16"
    service-type: external
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
  - name: redis
    port: 6379
    targetPort: 6379