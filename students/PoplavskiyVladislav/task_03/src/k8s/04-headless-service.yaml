apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
  namespace: state-lab03
  labels:
    org.bstu.course: "RSIOT"
    org.bstu.variant: "17"
spec:
  clusterIP: None # Важно для Headless Service
  selector:
    app: postgres
  ports:
  - port: 5432
    name: postgresql