apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: rental-app-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: rental-app
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
