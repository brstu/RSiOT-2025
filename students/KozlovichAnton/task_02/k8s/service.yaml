apiVersion: v1
kind: Service
metadata:
  name: app-kozlovich
  labels:
    app: app-kozlovich
spec:
  type: ClusterIP
  selector:
    app: app-kozlovich
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
