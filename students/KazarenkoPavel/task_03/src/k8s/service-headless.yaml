apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: state05
  labels:
    app: postgres
    org.bstu.student.id: "220008"
    org.bstu.group: "as-63"
    org.bstu.variant: "05"
spec:
  clusterIP: None
  selector:
    app: postgres
  ports:
  - port: 5432
    name: postgres
