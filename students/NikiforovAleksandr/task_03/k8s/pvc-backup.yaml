apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-backup-pvc
  namespace: state01
  labels:
    app: redis
    purpose: backup
    variant: "16"
spec:
  accessModes: ["ReadWriteOnce"]
  storageClassName: "fast"  
  resources:
    requests:
      storage: 5Gi