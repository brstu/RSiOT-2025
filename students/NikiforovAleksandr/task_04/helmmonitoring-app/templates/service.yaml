---
apiVersion: v1
kind: Service
metadata:
  name: memes-gallery
  namespace: app-memes-gallery
  labels:
    app: memes-gallery
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: memes-gallery