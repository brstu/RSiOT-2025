apiVersion: v1
kind: Service
metadata:
  name: chart-app18
  labels:
    app: app18
spec:
  selector:
    app: app18
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.service.port }}
