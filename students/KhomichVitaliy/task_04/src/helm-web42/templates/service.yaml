apiVersion: v1
kind: Service
metadata:
  name: rental-app
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: rental-app
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
