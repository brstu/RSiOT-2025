# Лабораторная работа №4

## Наблюдаемость и метрики (Prometheus + Grafana)

**Студент:** Грицук Дмитрий Юрьевич
**Группа:** АС-63
**ID:** 220006
**Вариант:** 3

---

## 📌 Параметры варианта

| Параметр | Значение |
|--------|---------|
| Префикс метрик | `app03_` |
| SLO доступность | 99.9% |
| SLO p95 latency | ≤ 200 ms |
| Alert | 5xx > 1% за 10 минут |

---

## 🧩 Описание работы

Разработать и развернуть веб-приложение с системой наблюдаемости на базе Prometheus, реализовать мониторинг SLO (Service Level Objectives), настроить алертинг и упаковать решение в Helm-чарт.

Создано Python-приложение с экспортом метрик Prometheus:

### App.py

- HTTP сервер на порту 8083
- Метрики Prometheus на порту 9090
- 3 основных эндпоинта: /, /health, /error
- Автоматическая симуляция латенси (P95=200ms) и ошибок (1% 5xx)

### Экспортируемые метрики

- app03_http_requests_total - счетчик HTTP запросов

- app03_http_request_duration_seconds - гистограмма времени ответа

- app03_active_connections - количество активных соединений

---

## 🚀 Быстрый старт

### 1️⃣ Запуск Minikube

```powershell
minikube start --cpus=4 --memory=8192
```

### 2️⃣ Установка monitoring stack

```powershell
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo update

helm install monitoring prometheus-community/kube-prometheus-stack \
  -n monitoring --create-namespace
```

### 3️⃣ Сборка образа приложения

```powershell
docker build -t web03:latest .

docker run -d -p 8083:8083 -p 9090:9090 --name web03 \
  -e STU_ID="220006" -e STU_GROUP="АС-63" \
  -e STU_VARIANT="03" -e STU_FULLNAME="Грицук Дмитрий Юрьевич" \
  web03:latest
```

### 4️⃣ Установка приложения через Helm

```powershell
helm install web03 ./helm/web03 -n app-monitoring --create-namespace
```

### 5️⃣ Доступ к UI

```powershell
# Grafana (логин: admin / prom-operator)
kubectl port-forward svc/monitoring-grafana 3000:80 -n monitoring

# Prometheus
kubectl port-forward svc/monitoring-kube-prometheus-prometheus 9090:9090 -n monitoring

# AlertManager
kubectl port-forward svc/monitoring-kube-prometheus-alertmanager 9093:9093 -n monitoring

# Приложение
kubectl port-forward svc/web03 8080:80 -n app-monitoring
```

---

## 🚨 Алерты (PrometheusRule)

### 1️⃣ HighErrorRate

- **Условие:** 5xx ошибки > 1% за 10 минут
- **Период:** 10 минут
- **Severity:** warning
- **Для студента:** Грицук Дмитрий Юрьевич (220006)

### 2️⃣ HighLatency

- **Условие:** P95 латенси > 200 ms
- **Период:** 10 минут
- **Severity:** warning
- **Для студента:** Грицук Дмитрий Юрьевич (220006)

### 3️⃣ LowAvailability

- **Условие:** Доступность < 99.9%
- **Период:** 10 минут
- **Severity:** critical
- **Для студента:** Грицук Дмитрий Юрьевич (220006)

---

## 📁 Структура проекта

task_04/
├── doc/
│   └── README.md
└── src/
    ├── app/
    │   ├── app.py
    │   ├── Dockerfile
    │   └── requirements.txt
    └── helm/
        └── web03/
        ├── Chart.yaml
        ├── values.yaml
        └── templates/
            ├── namespace.yaml
            ├── deployment.yaml
            ├── service.yaml
            ├── servicemonitor.yaml
            └── prometheusrule.yaml

---

## 🎯 Итог

Система обеспечивает полную наблюдаемость приложения, позволяет контролировать выполнение SLO и оперативно реагировать на нарушения через систему алертинга. Все требования лабораторной работы для варианта 3 выполнены в полном объеме.
