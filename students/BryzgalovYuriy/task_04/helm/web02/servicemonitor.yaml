---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mon-web02
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: web02
  namespaceSelector:
    matchNames:
      - {{.Values.namespace}}
  endpoints:
    - port: http
      path: /metrics
