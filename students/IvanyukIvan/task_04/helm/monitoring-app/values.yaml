---

replicaCount: 2

namespace: app32-ns

image:
  repository: app32
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 5000

ingress:
  enabled: true
  className: nginx
  hosts:
    - host: app32.example.local
      paths:
        - path: /
          pathType: Prefix

resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"

metrics:
  prefix: app32
  serviceMonitor:
    enabled: true
    interval: 15s