apiVersion: v1
kind: Service
metadata:
  name: {{ include "app09.fullname" . }}
  labels:
    app: {{ include "app09.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "app09.name" . }}
  ports:
    - name: http-metrics
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
