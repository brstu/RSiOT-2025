apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    app: web03
    variant: "03"
    org.bstu.student.id: "{{ .Values.env.STU_ID }}"
    org.bstu.student.fullname: "{{ .Values.env.STU_FULLNAME }}"