# pvc-redis.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-redis-0
  namespace: state24
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: premium
