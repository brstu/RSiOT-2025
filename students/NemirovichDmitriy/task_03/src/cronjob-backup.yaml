---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-as64-220050-v37
  namespace: state-as64-220050-v37
  labels:
    org.bstu.student.id: "220050"
    org.bstu.group: "АС-64"
    org.bstu.variant: "37"
spec:
  schedule: "5 6 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: backup
              image: postgres:15
              command:
                - /bin/sh
                - -c
                - |
                  echo "Backup started at $(date)"
                  pg_dump -U postgres -d testdb > /tmp/backup.sql
                  echo "Backup completed"
          restartPolicy: OnFailure
