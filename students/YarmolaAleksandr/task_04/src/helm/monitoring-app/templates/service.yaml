---
apiVersion: v1
kind: Service
metadata:
  name: monitoring-app
  namespace: monitoring-app
  labels:
    app: monitoring-app
    monitoring: "true"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: monitoring-app
