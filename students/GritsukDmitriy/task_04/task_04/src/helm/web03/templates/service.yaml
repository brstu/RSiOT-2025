apiVersion: v1
kind: Service
metadata:
  name: web03
  namespace: {{ .Values.namespace }}
  labels:
    app: web03
    monitoring: "true"
spec:
  selector:
    app: web03
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: metrics
    port: 9090
    targetPort: metrics