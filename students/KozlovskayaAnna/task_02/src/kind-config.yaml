---
# Kind конфигурация для локального тестирования
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: lab02-cluster
nodes:
  - role: control-plane
    kubeadmConfigPatches:
      - |
        kind: InitConfiguration
        nodeRegistration:
          kubeletExtraArgs:
            node-labels: "org.bstu.variant=8,org.bstu.student=as63-220012-v8"
    extraPortMappings:
      - containerPort: 30000
        hostPort: 8094
        protocol: TCP
