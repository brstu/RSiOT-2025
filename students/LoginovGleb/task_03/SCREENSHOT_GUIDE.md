# 📸 Руководство по созданию скриншотов для Лабораторной работы №3

Данное руководство содержит пошаговые инструкции для создания всех 16 необходимых скриншотов.

## Предварительные требования

- Установлен и настроен `kubectl`
- Доступ к Kubernetes кластеру
- Инструмент для создания скриншотов (Windows: `Win + Shift + S`, Linux: `gnome-screenshot`, Mac: `Cmd + Shift + 4`)

---Я

## Фаза 1: Развёртывание инфраструктуры и StatefulSet

### Скриншот 1: `01_namespace_created.png` - Создан namespace

**Что делать:**

1. Перейдите в директорию проекта:

```bash
cd c:\Users\kseni\Documents\Универ\4-курс\РСОТ\RSiOT-2025 (Loginov)\students\LoginovGleb\task_03\src
```

1. Создайте namespace:

```bash
kubectl apply -f k8s/namespace.yaml
```

1. Проверьте создание namespace:

```bash
kubectl get namespace state-as63-220018-v14
```

1. **Сделайте скриншот** терминала с выводом команды, показывающим:
   - Команду `kubectl get namespace state-as63-220018-v14`
   - Статус namespace (должен быть `Active`)

---

### Скриншот 2: `02_pvc_bound.png` - PVC в состоянии Bound

**Что делать:**

1. Разверните storage class и PVC:

```bash
kubectl apply -f k8s/storage-class.yaml
kubectl apply -f k8s/backup-pvc.yaml
```

1. Разверните StatefulSet (он создаст PVC автоматически):

```bash
kubectl apply -f k8s/secret.yaml
kubectl apply -f k8s/headless-service.yaml
kubectl apply -f k8s/statefulset.yaml
```

1. Подождите несколько секунд и проверьте PVC:

```bash
kubectl get pvc -n state-as63-220018-v14
```

1. **Сделайте скриншот** терминала с выводом, показывающим:
   - Команду `kubectl get pvc -n state-as63-220018-v14`
   - PVC с именем `redis-data-db-as63-220018-v14-0` в статусе `Bound`
   - Размер PVC (1Gi)

---

### Скриншот 3: `03_statefulset_ready.png` - StatefulSet готов

**Что делать:**

1. Проверьте статус StatefulSet:

```bash
kubectl get statefulset -n state-as63-220018-v14
```

1. **Сделайте скриншот** терминала с выводом, показывающим:
   - Команду `kubectl get statefulset -n state-as63-220018-v14`
   - StatefulSet `db-as63-220018-v14` с `READY 1/1`

Альтернативно, для более подробной информации:

```bash
kubectl describe statefulset db-as63-220018-v14 -n state-as63-220018-v14
```

---

### Скриншот 4: `04_pods_running.png` - Поды запущены

**Что делать:**

1. Проверьте статус подов:

```bash
kubectl get pods -n state-as63-220018-v14
```

1. **Сделайте скриншот** терминала с выводом, показывающим:
   - Команду `kubectl get pods -n state-as63-220018-v14`
   - Под `db-as63-220018-v14-0` в статусе `Running` с `READY 1/1`

Для дополнительной информации можно выполнить:

```bash
kubectl get pods -n state-as63-220018-v14 -o wide
```

---

## Фаза 2: Проверка сохранности данных

### Скриншот 5: `05_data_before_restart.png` - Данные до перезапуска пода

**Что делать:**

1. Подключитесь к Redis CLI:

```bash
kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- redis-cli
```

1. В консоли Redis выполните команды для создания тестовых данных:

```redis
SET test:key1 "Loginov Gleb"
SET test:key2 "AS-63-220018"
SET test:key3 "Lab03-Persistence-Test"
SET test:timestamp "2025-12-20T12:00:00"
```

1. Проверьте созданные данные:

```redis
KEYS test:*
GET test:key1
GET test:key2
GET test:key3
GET test:timestamp
```

1. **Сделайте скриншот** терминала Redis CLI, показывающий:
   - Команды `KEYS test:*` с выводом всех 4 ключей
   - Команды `GET` для всех ключей с их значениями

2. Выйдите из Redis CLI:

```redis
exit
```

---

### Скриншот 6: `06_pod_deletion.png` - Удаление пода

**Что делать:**

1. Удалите под:

```bash
kubectl delete pod db-as63-220018-v14-0 -n state-as63-220018-v14
```

1. **Сделайте скриншот** терминала сразу после выполнения команды, показывающий:
   - Команду `kubectl delete pod db-as63-220018-v14-0 -n state-as63-220018-v14`
   - Сообщение `pod "db-as63-220018-v14-0" deleted`

---

### Скриншот 7: `07_pod_recreated.png` - Под пересоздан

**Что делать:**

1. Подождите 5-10 секунд и проверьте статус пода:

```bash
kubectl get pods -n state-as63-220018-v14
```

1. **Сделайте скриншот** терминала, показывающий:
   - Команду `kubectl get pods -n state-as63-220018-v14`
   - Под `db-as63-220018-v14-0` снова в статусе `Running`
   - Обратите внимание на колонку `AGE` (должен показывать недавнее время создания, например "30s")

Дополнительно можно показать описание пода:

```bash
kubectl describe pod db-as63-220018-v14-0 -n state-as63-220018-v14 | grep "Started:"
```

---

### Скриншот 8: `08_data_after_restart.png` - Данные после перезапуска

**Что делать:**

1. Снова подключитесь к Redis CLI:

```bash
kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- redis-cli
```

1. Проверьте, что данные сохранились:

```redis
KEYS test:*
GET test:key1
GET test:key2
GET test:key3
GET test:timestamp
```

1. **Сделайте скриншот** терминала Redis CLI, показывающий:
   - Команды `KEYS test:*` с выводом всех 4 ключей
   - Команды `GET` для всех ключей с их **прежними** значениями
   - Это доказывает, что данные сохранились после перезапуска пода

2. Выйдите из Redis CLI:

```redis
exit
```

---

## Фаза 3: Система резервного копирования

### Скриншот 9: `09_backup_cronjob_created.png` - Создан CronJob для бэкапа

**Что делать:**

1. Разверните компоненты резервного копирования:

```bash
kubectl apply -f k8s/rbac-restore.yaml
kubectl apply -f k8s/backup-configmap.yaml
kubectl apply -f k8s/cronjob-backup.yaml
```

1. Проверьте создание CronJob:

```bash
kubectl get cronjob -n state-as63-220018-v14
```

1. **Сделайте скриншот** терминала, показывающий:
   - Команду `kubectl get cronjob -n state-as63-220018-v14`
   - CronJob `redis-backup-as63-220018-v14` с расписанием и статусом

Для более подробной информации:

```bash
kubectl describe cronjob redis-backup-as63-220018-v14 -n state-as63-220018-v14
```

---

### Скриншот 10: `10_backup_job_success.png` - Задача резервного копирования выполнена

**Что делать:**

1. Запустите резервное копирование вручную:

```bash
kubectl create job --from=cronjob/redis-backup-as63-220018-v14 manual-backup-001 -n state-as63-220018-v14
```

1. Подождите несколько секунд и проверьте статус Job:

```bash
kubectl get jobs -n state-as63-220018-v14
```

1. Дождитесь, пока статус не станет `COMPLETIONS 1/1`

2. **Сделайте скриншот** терминала, показывающий:
   - Команду `kubectl get jobs -n state-as63-220018-v14`
   - Job `manual-backup-001` с `COMPLETIONS 1/1` (успешно завершён)

Дополнительно можно показать логи:

```bash
kubectl logs job/manual-backup-001 -n state-as63-220018-v14
```

---

### Скриншот 11: `11_backup_files_list.png` - Список файлов резервных копий

**Что делать:**

1. Проверьте список файлов резервных копий на PVC:

```bash
kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- ls -lh /backup
```

1. **Сделайте скриншот** терминала, показывающий:
   - Команду `kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- ls -lh /backup`
   - Список файлов `.rdb` с датами и размерами

Альтернативный способ (если PVC смонтирован в backup-under):

```bash
kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- sh -c "ls -lh /backup/*.rdb"
```

---

## Фаза 4: Система восстановления

### Скриншот 12: `12_data_before_deletion.png` - Данные до удаления

**Что делать:**

1. Подключитесь к Redis CLI:

```bash
kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- redis-cli
```

1. Проверьте существующие данные:

```redis
KEYS *
DBSIZE
```

1. **Сделайте скриншот** Redis CLI, показывающий:
   - Команду `KEYS *` с полным списком ключей
   - Команду `DBSIZE` с количеством ключей (должно быть минимум 4)

2. НЕ выходите из Redis CLI (нужно для следующего скриншота)

---

### Скриншот 13: `13_data_deleted.png` - Данные удалены (FLUSHALL)

**Что делать:**

1. В том же сеансе Redis CLI выполните:

```redis
FLUSHALL
KEYS *
DBSIZE
```

1. **Сделайте скриншот** Redis CLI, показывающий:
   - Команду `FLUSHALL` с ответом `OK`
   - Команду `KEYS *` с пустым выводом
   - Команду `DBSIZE` с результатом `(integer) 0`
   - Это доказывает, что все данные удалены

2. Выйдите из Redis CLI:

```redis
exit
```

---

### Скриншот 14: `14_restore_job_running.png` - Задача восстановления выполняется

**Что делать:**

1. Разверните конфигурацию для восстановления:

```bash
kubectl apply -f k8s/restore-configmap.yaml
```

1. Создайте Job для восстановления:

```bash
kubectl apply -f k8s/job-restore.yaml
```

1. **Быстро** проверьте статус Job (нужно успеть поймать статус "Running"):

```bash
kubectl get jobs -n state-as63-220018-v14 -w
```

1. **Сделайте скриншот** терминала, показывающий:
   - Команду `kubectl get jobs -n state-as63-220018-v14`
   - Job восстановления в статусе `Running` или с `COMPLETIONS 0/1`

Если Job выполняется слишком быстро, можно показать логи:

```bash
kubectl get pods -n state-as63-220018-v14 | grep restore
```

---

### Скриншот 15: `15_restore_job_completed.png` - Задача восстановления завершена

**Что делать:**

1. Дождитесь завершения Job восстановления:

```bash
kubectl get jobs -n state-as63-220018-v14
```

1. **Сделайте скриншот** терминала, показывающий:
   - Команду `kubectl get jobs -n state-as63-220018-v14`
   - Job восстановления с `COMPLETIONS 1/1` (успешно завершён)

Для проверки логов:

```bash
kubectl logs -l job-name=redis-restore-as63-220018-v14 -n state-as63-220018-v14
```

---

### Скриншот 16: `16_data_after_restore.png` - Данные после восстановления

**Что делать:**

1. Подключитесь к Redis CLI:

```bash
kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- redis-cli
```

1. Проверьте восстановленные данные:

```redis
KEYS test:*
GET test:key1
GET test:key2
GET test:key3
GET test:timestamp
DBSIZE
```

1. **Сделайте скриншот** Redis CLI, показывающий:
   - Команду `KEYS test:*` с восстановленными ключами
   - Команды `GET` для всех ключей с их **оригинальными** значениями
   - Команду `DBSIZE` показывающую количество восстановленных ключей
   - Это доказывает, что данные успешно восстановлены из резервной копии

2. Выйдите из Redis CLI:

```redis
exit
```

---

## 📁 Сохранение скриншотов

После создания всех скриншотов:

1. Переименуйте файлы согласно списку выше
2. Поместите их в директорию:

```
c:\Users\kseni\Documents\Универ\4-курс\РСОТ\RSiOT-2025 (Loginov)\students\LoginovGleb\task_03\doc\screenshots\
```

1. Убедитесь, что все 16 файлов на месте:
   - 01_namespace_created.png
   - 02_pvc_bound.png
   - 03_statefulset_ready.png
   - 04_pods_running.png
   - 05_data_before_restart.png
   - 06_pod_deletion.png
   - 07_pod_recreated.png
   - 08_data_after_restart.png
   - 09_backup_cronjob_created.png
   - 10_backup_job_success.png
   - 11_backup_files_list.png
   - 12_data_before_deletion.png
   - 13_data_deleted.png
   - 14_restore_job_running.png
   - 15_restore_job_completed.png
   - 16_data_after_restore.png

---

## 🧹 Очистка после тестирования

После создания всех скриншотов можно удалить все ресурсы:

```bash
kubectl delete namespace state-as63-220018-v14
```

Или используя Makefile:

```bash
make clean
```

---

## ⚠️ Важные замечания

1. **Имена ресурсов**: Все команды используют имена из вашей конфигурации (`state-as63-220018-v14`, `db-as63-220018-v14-0`)
2. **Терминал**: Используйте PowerShell или CMD на Windows
3. **Размер окна**: Убедитесь, что терминал достаточно широкий для корректного отображения всех колонок
4. **Качество**: Сохраняйте скриншоты в формате PNG для лучшего качества
5. **Видимость команд**: На каждом скриншоте должна быть видна выполненная команда и её результат
6. **Время выполнения**: Некоторые операции (создание пода, выполнение job) могут занять 10-30 секунд

---

## 🆘 Решение проблем

### Под не запускается

```bash
kubectl describe pod db-as63-220018-v14-0 -n state-as63-220018-v14
kubectl logs db-as63-220018-v14-0 -n state-as63-220018-v14
```

### PVC не переходит в Bound

```bash
kubectl describe pvc redis-data-db-as63-220018-v14-0 -n state-as63-220018-v14
kubectl get storageclass
```

### Job не завершается

```bash
kubectl get pods -n state-as63-220018-v14
kubectl logs <pod-name> -n state-as63-220018-v14
```

### Данные не восстанавливаются

```bash
kubectl logs -l job-name=redis-restore-as63-220018-v14 -n state-as63-220018-v14
kubectl exec -it db-as63-220018-v14-0 -n state-as63-220018-v14 -- ls -lh /backup
```

---

**Удачи с созданием скриншотов! 🚀**
