prometheus:
  prometheusSpec:
    serviceMonitorSelectorNilUsesHelmValues: false   # отключает фильтр по Helm-меткам
    serviceMonitorNamespaceSelector: {}              # разрешает ВСЕ namespace
    podMonitorSelectorNilUsesHelmValues: false
    podMonitorNamespaceSelector: {}
    ruleSelectorNilUsesHelmValues: false
    ruleNamespaceSelector: {}

grafana:
  adminPassword: admin   # чтобы не потерять пароль